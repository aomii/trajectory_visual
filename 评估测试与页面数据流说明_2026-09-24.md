# 评估测试与页面数据流说明

## 1. 两类测试的边界

| 类型 | 测试类 | 结果去向 | 数据库表 |
|---|---|---|---|
| 论文补充实验 | `SupplementExperimentTest` | `src/main/resources/export/supplement/` 下的 CSV、Markdown | 通常不读、不写数据库；停留对照和凸包实验仅读 `trajectory_waybill` 元数据 |
| 系统评估实验 | `TrajectoryChapter6ExperimentTest` | MySQL 评估表，供前端评估页查询 | 写入 `trajectory_eval_*`；失败明细写 `trajectory_eval_error_record` |

两类测试都从 `application-local.yml` 的 `trajectory.source.full-data-dir` 读取 `source_data_full`。当前目录位于后端项目根目录，且已被 Git 忽略。

## 2. 论文补充实验：`SupplementExperimentTest`

该类的所有方法共享 `trajectory.experiment.*` 参数、`trajectory.source.full-data-dir` 源目录和 `supplement.outDir` 导出目录。默认导出到 `src/main/resources/export/supplement/`。

| 方法 | 作用与主要输出 | 涉及数据库表 |
|---|---|---|
| `runAllSupplementExperiments` | 顺序执行本表全部补充实验，生成完整论文附表 | 仅读 `trajectory_waybill`，因为其中包含停留对照和凸包实验 |
| `runStopRecognitionCompare` | 表 6-7：静态合并与 ST-DBSCAN 的候选停留识别对比 | 读 `trajectory_waybill` 的收发坐标、装卸货时间 |
| `runLosslessBenchmark` | 表 6-12、表 6-13：无损层体积、编码/解码基准 | 不涉及数据库 |
| `runEndToEndBenchmark` | `table6_end_to_end_common_encoder.{csv,md}`：本文与 DP、DPS、TD-TR、Trajic 均接入同一无损编码器后的端到端比较 | 不涉及数据库 |
| `runP1SensitivityBenchmark` | `table6_p1_*.{csv,md}`：`ε_same`、`d_min`、断线续接、`T_long` 和 ST-DBSCAN 参数敏感性 | 不涉及数据库 |
| `runEntropyBenchmark` | 表 6-14：偏移量经验熵与编码字节率 | 不涉及数据库 |
| `runBlockLengthSweep` | 表 6-16：分块时长与编码/查询权衡 | 不涉及数据库 |
| `runHullShapeBenchmark` | 表 6-18：凸包保形度、压缩失真和速度比 | 读 `trajectory_waybill` 的收发坐标 |

运行 P1 时，在 IDEA 中选择 `SupplementExperimentTest#runP1SensitivityBenchmark`。不再保留独立的 `P1SensitivityBenchmarkTest`，从而避免其拥有单独的源目录和硬编码参数。

## 3. 系统评估实验：`TrajectoryChapter6ExperimentTest`

每个方法均新建一个 `trajectory_eval_run` 批次；不会覆盖旧批次。前端默认优先选择包含完整多算法对比结果的最新成功批次。

| 方法 | 写入表 | 页面用途 |
|---|---|---|
| `runAllExperiments` | 调用以下全部实验，写入相应全部评估表 | 刷新所有评估页 |
| `runStopRecognitionExperiment` | `trajectory_eval_run`、`trajectory_eval_error_record` | 运行信息和论文统计 |
| `runLossyCompressionCompareExperiment` | `trajectory_eval_run`、`trajectory_eval_algorithm_result`、`trajectory_eval_waybill_result`、`trajectory_eval_error_record` | Dashboard 和算法对比页的核心数据；含五种算法的统一编码器端到端指标 |
| `runLosslessEncodingExperiment` | `trajectory_eval_run`、`trajectory_eval_algorithm_result`、`trajectory_eval_waybill_result`、`trajectory_eval_error_record` | 本文方法无损层/编码指标 |
| `runPartialDecompressionExperiment` | `trajectory_eval_run`、`trajectory_eval_algorithm_result`、`trajectory_eval_waybill_result`、`trajectory_eval_error_record` | 本文方法部分解压指标 |
| `runAblationExperiment` | `trajectory_eval_run`、`trajectory_eval_ablation_result`、`trajectory_eval_error_record` | 消融页 |
| `runParameterSensitivityExperiment` | `trajectory_eval_run`、`trajectory_eval_param_result`、`trajectory_eval_error_record` | 参数敏感性页、Dashboard 的分块曲线 |

### 评估表职责

| 表 | 内容 |
|---|---|
| `trajectory_waybill` | 运单元数据；工作台列表使用，补充实验中也提供收发坐标和装卸货时间 |
| `trajectory_eval_run` | 一次评估运行的批次、参数快照、数据规模、状态 |
| `trajectory_eval_algorithm_result` | 一批次 × 一算法的聚合指标 |
| `trajectory_eval_waybill_result` | 一批次 × 一运单 × 一算法的明细指标 |
| `trajectory_eval_ablation_result` | 一批次 × 一个消融变体的结果 |
| `trajectory_eval_param_result` | 一批次 × 参数值 × 算法的敏感性结果 |
| `trajectory_eval_error_record` | 单运单失败记录，不作为正常图表数据 |

## 4. 前端页面的数据来源

| 页面 | 读取接口 | 读取的数据源 |
|---|---|---|
| `/workbench` 轨迹压缩工作台 | `GET /api/visual/waybill/page`、`/trajectory/original/{id}`、`/compressed/{id}`、`/chunks/{id}`、`/config`；压缩操作使用 `POST /compress/{id}`、`/compress-all` | MySQL `trajectory_waybill`；MongoDB `trajectory_chunk`；原始轨迹 JSON（`source_data_full`） |
| `/timewindow` 时间窗口检索 | `POST /api/visual/trajectory/search-by-time` | MongoDB `trajectory_chunk`；必要的运单信息来自 MySQL `trajectory_waybill` |
| `/dashboard` 实验评估中心 | `GET /api/visual/evaluation/runs`、`/dashboard` | MySQL `trajectory_eval_run`、`trajectory_eval_algorithm_result`、`trajectory_eval_waybill_result`；无对应数据时回退读取 `trajectory_eval_ablation_result`、`trajectory_eval_param_result` |
| `/compare` 算法压缩效果对比 | `GET /api/visual/evaluation/algorithm-compare`、`/waybill/{id}` | MySQL `trajectory_eval_run`、`trajectory_eval_algorithm_result`、`trajectory_eval_waybill_result` |
| `/compare` 地图下钻 | `GET /api/visual/trajectory/algorithm-tracks/{id}` | 实时读取 `source_data_full` 并执行算法；不读 `trajectory_eval_*` 表 |
| `/ablation` 消融与参数敏感性 | `GET /api/visual/evaluation/ablation`、`/parameter-sensitivity`、`/partial-decompression` | MySQL `trajectory_eval_ablation_result`、`trajectory_eval_param_result`，以及聚合指标表 `trajectory_eval_algorithm_result` |

## 5. 常用运行入口

| 目标 | 运行方法 |
|---|---|
| 仅更新论文端到端表 | `SupplementExperimentTest#runEndToEndBenchmark` |
| 仅更新 P1 参数敏感性表 | `SupplementExperimentTest#runP1SensitivityBenchmark` |
| 更新网页五算法对比数据 | `TrajectoryChapter6ExperimentTest#runLossyCompressionCompareExperiment` |
| 更新消融或参数页 | 分别运行 `runAblationExperiment`、`runParameterSensitivityExperiment` |
| 全量重跑系统评估 | `TrajectoryChapter6ExperimentTest#runAllExperiments` |
