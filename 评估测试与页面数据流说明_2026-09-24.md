# 评估测试与页面数据流说明

## 1. 先看结论

系统中存在两条彼此独立的评估链路：

1. `com.logicompress.supplement` 下的测试类用于生成论文可引用的 CSV 和 Markdown 表格，**不连接 MySQL 或 MongoDB**；
2. `com.fkhwl.nfs.biz.experiment` 下的 Spring 测试类用于生成可视化系统页面数据，结果写入 MySQL 的 `trajectory_eval_*` 表。

因此，运行 `EndToEndBenchmarkTest#runFullData` 后，只会更新 `src/main/resources/export/supplement/table6_end_to_end_common_encoder.{csv,md}`，不会让 Dashboard 或算法对比页出现新数据。要刷新评估页面，必须运行 `Chapter6FullExperimentTest#runLossyCompareOnly`。

## 2. EndToEndBenchmarkTest 为什么会生成全 0

原测试类的默认源目录是 `../00数据处理-260909/source_data_full`。当 IDEA 以 `trajectory-visual-server` 为工作目录启动测试时，该相对路径实际指向：

```text
05可视化系统/00数据处理-260909/source_data_full
```

这个目录不存在。加载器原先将“不存在目录”视为“没有轨迹文件”，因此生成了只有表头或所有值为 0 的结果，而没有抛异常。

本次已修复为正确默认路径：

```text
../../00数据处理-260909/source_data_full
```

并新增“有效运单数必须大于 0”的失败保护。之后路径配错会直接报出完整绝对路径，而不会继续生成错误的 0 值表。

在 IDEA 中建议检查 Run Configuration：

- Working directory：`05可视化系统/trajectory-visual-server`；
- VM options 可留空，测试会使用修复后的默认路径；
- 若工作目录不是模块目录，设置：

```text
-De2e.sourceDir=D:\aoming\电科\毕业论文260828-开题-codex\00数据处理-260909\source_data_full
```

成功时控制台会输出：`[e2e] 已加载有效运单 N 条`。`N` 应为 5,224（全量）或你设置的限制数附近；若仍为 0，应停止使用该输出文件。

## 3. 论文表格测试链路

```text
source_data_full 的 track_*.json
        ↓
SupplementContext.load
  漂移清洗 + 候选单元识别
        ↓
EndToEndBenchmark
  本文 / DP / DPS / TD-TR / Trajic
  各自有损输出 → 相同 BlockEncoder → DEFLATE
        ↓
table6_end_to_end_common_encoder.csv / .md
```

### 3.1 EndToEndBenchmarkTest#runFullData

**用途**：生成论文层次三的公平比较表。

**核心口径**：五种方法从同一清洗轨迹出发，每种方法的保留点都送入相同的 1 小时分块、差分、5 位分片与 DEFLATE 编码器。主要指标为：

```text
全局端到端 CR = 全部清洗后点列规范文本字节 / 全部编码负载字节
```

**输出文件**：

- `trajectory-visual-server/src/main/resources/export/supplement/table6_end_to_end_common_encoder.csv`
- `trajectory-visual-server/src/main/resources/export/supplement/table6_end_to_end_common_encoder.md`

**不涉及数据库表**：该测试只读 JSON、写 CSV/Markdown。

### 3.2 P1SensitivityBenchmarkTest#runFullData

**用途**：输出候选单元参数敏感性，包括 `ε_same`、`d_min`、跨断线续接上限、`T_long` 和 ST-DBSCAN 参数扫描。

**输出文件**：`table6_p1_*.csv` 与 `table6_p1_*.md`。

**不涉及数据库表**：同样只读 JSON、写表格文件。该测试也已修复默认路径和空数据保护。

### 3.3 SupplementExperimentTest

| 测试方法 | 主要输出 | 作用 |
|---|---|---|
| `runStopRecognitionCompare` | 停留识别比较表 | 本文候选单元与 ST-DBSCAN 等对比 |
| `runLosslessBenchmark` | `table6_12_*`、`table6_13_*` | 本文保留点的无损编码、解码和部分解压微基准 |
| `runEndToEndBenchmark` | `table6_end_to_end_common_encoder.*` | 与独立 `EndToEndBenchmarkTest` 相同的端到端比较 |
| `runEntropyBenchmark` | 经验熵表 | 解释偏移量编码收益 |
| `runBlockLengthSweep` | 块长扫描表 | 编码负载和查询读取比例权衡 |
| `runHullShapeBenchmark` | 凸包保形度表 | 几何覆盖范围损失 |

`runAllSupplementExperiments` 会依次执行上述所有补充实验，耗时和内存占用均较高；仅更新端到端表时优先执行 `EndToEndBenchmarkTest#runFullData`。

## 4. 写入可视化系统数据库的测试链路

```text
Chapter6FullExperimentTest#runLossyCompareOnly
        ↓
Chapter6ExperimentRunner.runLossyCompressionCompare
        ↓
CompressionPipeline.process
  清洗 → 候选单元 → 有损抽稀
  本文、DP、DPS、TD-TR、Trajic
  全部接入相同 BlockEncoder
        ↓
trajectory_eval_run
trajectory_eval_algorithm_result
trajectory_eval_waybill_result
        ↓
DashboardView / CompareView
```

### 4.1 最重要的页面刷新测试

`Chapter6FullExperimentTest#runLossyCompareOnly`

它是更新 Dashboard 和“算法压缩效果对比”页面所必需的测试。当前实现会为每个有效运单分别计算五种算法的：

- 有损层：保留点数、`CR_lossy`、PED、SED、候选锚点 SR、候选单元完整率、观测跨度保真度；
- 无损层：保留点规范文本字节、编码负载、编码与解码耗时、部分读取比例；
- 层次三：`cr_e2e`（单运单端到端 CR）与 `cr_e2e_global`（全局端到端 CR）。

该测试需要 MySQL 正常运行，并在已有数据库中先执行：

```text
sql/upgrade_20260923_e2e_baseline.sql
```

旧批次没有 `cr_e2e` 数据；运行完成后会新增一个批次，不覆盖历史批次。页面默认优先选取含完整多算法数据的最新成功批次。

### 4.2 其他数据库测试

| 测试方法 | 写入重点 | 主要页面 |
|---|---|---|
| `runStopRecognitionExperiment` | `trajectory_eval_run` 的识别汇总信息 | Dashboard 的运行信息与论文分析 |
| `runLossyCompressionCompareExperiment` | `run`、`algorithm_result`、`waybill_result` | Dashboard、算法对比页 |
| `runLosslessEncodingExperiment` | 本文方法的聚合/明细行 | Dashboard 的本文编码指标 |
| `runPartialDecompressionExperiment` | 本文方法的聚合/明细行 | Dashboard 的部分解压指标 |
| `runAblationExperiment` | `trajectory_eval_ablation_result` | 消融与参数敏感性页 |
| `runParameterSensitivityExperiment` | `trajectory_eval_param_result` | 消融与参数敏感性页、Dashboard 的块长曲线 |
| `runAllExperiments` | 上述全部 | 全部评估页 |

## 5. MySQL 评估表说明

| 表 | 粒度 | 写入测试 | 前端用途 |
|---|---|---|---|
| `trajectory_eval_run` | 一次实验批次一行 | 所有 Spring 实验测试 | 批次选择、状态、数据规模、参数快照 |
| `trajectory_eval_algorithm_result` | 一批次 × 一算法一行 | 多算法主实验、本文单算法实验 | Dashboard/Compare 的聚合柱状图、KPI、端到端 CR |
| `trajectory_eval_waybill_result` | 一批次 × 一运单 × 一算法一行 | 多算法主实验、本文单算法实验 | Compare 单运单下钻；均值、中位数、最小值、最大值 |
| `trajectory_eval_ablation_result` | 一批次 × 一消融变体一行 | 消融实验 | AblationParamView 的消融表和图 |
| `trajectory_eval_param_result` | 一批次 × 参数值 × 算法一行 | 参数敏感性实验 | AblationParamView 的 DP 容差与块长曲线 |
| `trajectory_eval_error_record` | 一批次 × 失败运单一行 | 所有 Spring 实验测试 | 失败诊断接口，不作为正常图表数据 |
| `trajectory_waybill` | 一运单一行 | 运单导入接口 | 工作台运单列表、运单元数据；不直接提供第 6 章统计 |

`cr_e2e_global` 与 `input_bytes` 位于 `trajectory_eval_algorithm_result`；`cr_e2e` 与 `input_bytes` 位于 `trajectory_eval_waybill_result`。它们由 `upgrade_20260923_e2e_baseline.sql` 新增。

## 6. 前端页面读取的数据

| 前端页面 | 主要接口 | MySQL / MongoDB 数据源 | 说明 |
|---|---|---|---|
| `/dashboard` 实验评估中心 | `/evaluation/dashboard`、`/evaluation/runs` | MySQL：`run`、`algorithm_result`、`waybill_result`；并回退读取 `ablation_result`、`param_result` | 首屏 KPI、端到端对比、语义/误差图、块长曲线、动态结论 |
| `/compare` 算法压缩效果对比 | `/evaluation/algorithm-compare`、`/evaluation/waybill/{id}` | MySQL：`algorithm_result`、`waybill_result` | 聚合对比和单运单统计；默认指标为全局端到端编码负载压缩率 |
| `/compare` 的地图下钻 | `/trajectory/algorithm-tracks/{id}` | 不读取评估表；实时读取轨迹 JSON 并执行算法 | 展示本文与四个基线的保留点位置；用于解释差异，不是数据库聚合结果 |
| `/ablation` 消融与参数敏感性 | `/evaluation/ablation`、`/evaluation/parameter-sensitivity` | MySQL：`ablation_result`、`param_result` | 消融效果、DP 容差扫描、分块时长扫描 |
| `/workbench` 轨迹压缩工作台 | `/waybill/*`、`/trajectory/*` | MySQL：`trajectory_waybill`；MongoDB：`trajectory_chunk`；原始轨迹来自 JSON | 单运单在线压缩、停留锚点和分块展示，不使用第 6 章评估表 |
| `/timewindow` 时间窗口部分检索 | `/trajectory/search-by-time` | MongoDB：`trajectory_chunk`；MySQL：`trajectory_waybill` | 查询已在线压缩的分块，不使用第 6 章评估表 |

## 7. 推荐操作顺序

1. 在 MySQL 执行 `sql/upgrade_20260923_e2e_baseline.sql`；
2. 在 IDEA 中重新运行 `EndToEndBenchmarkTest#runFullData`，确认控制台打印有效运单数后，再查看论文端 `.md`；
3. 如需更新网页评估数据，运行 `Chapter6FullExperimentTest#runLossyCompareOnly`；
4. 重启后端，打开 `/dashboard`，在批次下拉框确认已选中新的“多算法三层评估”成功批次；
5. 打开 `/compare`，检查默认“全局端到端编码负载压缩率”下本文、DP、DPS、TD-TR、Trajic 都有数值。

## 8. 快速排错

| 现象 | 首先检查 | 处理方式 |
|---|---|---|
| 论文端到端表全为 0 | EndToEnd 控制台是否打印“已加载有效运单” | 检查工作目录；或设置 `-De2e.sourceDir` 为绝对路径 |
| 页面仍显示旧口径或空值 | 数据库是否执行 20260923 升级脚本；是否新跑主实验 | 执行升级脚本，再跑 `runLossyCompareOnly`，刷新批次 |
| Dashboard 只出现本文一条线 | 当前选中的是 6.4/6.5 单算法批次 | 在批次选择框切换到“多算法三层评估”批次，或不传 `runId` 让后端自动选择 |
| Compare 的地图与表格数字不同 | 地图是实时算法下钻，表格是指定批次数据库结果 | 确认两者参数一致；论文统计以数据库主实验和导出表为准 |
| 消融/参数页为空 | 对应实验尚未运行或默认批无相关行 | 运行 `runAblationExperiment` 或 `runParameterSensitivityExperiment`；服务会回退到最近有该类数据的批次 |
